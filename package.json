{
  "name": "docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "2.0.20"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon -L index.js",
    "build_image": "docker build -t node-app-image .",
    "run_docker": "docker run -v $(pwd):/app -p 3000:3000 -d --name node-app node-app-image"
  },
  "author": "",
  "license": "ISC"
}
